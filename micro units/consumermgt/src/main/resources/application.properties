server.port=8585


### Spring properties
# Service registers under this name
spring.application.name= suppliermgt

# Discovery Server Access
eureka.client.serviceUrl.defaultZone= http://localhost:8484/eureka/

spring.mvc.view.prefix = /WEB-INF/views/
spring.mvc.view.suffix = .jsp


# ===============================
# = DATA SOURCE
# ===============================
spring.datasource.driver-class-name=org.mysqldb.jdbcDriver
spring.datasource.url = jdbc:mysqldb:mem:employeedb
spring.datasource.username = Roopsai
spring.datasource.password=Roopsai@437

# ===============================
# JPA / HIBERNATE / with Mysql
# ===============================
spring.jpa.show-sql = false
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect
hibernate.format_sql=true;
# ===============================